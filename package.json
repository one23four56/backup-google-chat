{
  "dependencies": {
    "@types/express": "^4.17.13",
    "body-parser": "^1.19.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "nodemailer": "^6.7.1",
    "socket.io": "^4.4.0",
    "typescript": "^4.5.2",
    "uuid": "^8.3.2"
  },
  "scripts": {
    "start": "echo Running... && node index.js",
    "build": "echo Updating Dependencies... && npm ci --silent && echo Dependencies Updated && echo Building... && npx tsc index.ts && echo Build Done!",
    "test": "npm run --silent build && npm --silent start"
  }
}
